<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.think2exam.projectt2e">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.CourseDetailsActivity"></activity>
        <activity android:name=".ui.activities.QuizResultsActivity" />
        <activity android:name=".ui.activities.AboutActivity" />
        <activity android:name=".ui.activities.PointsSummaryActivity" />
        <activity android:name=".ui.activities.EditProfileActivity" />
        <activity android:name=".ui.activities.QuizActivity" />
        <activity android:name=".ui.activities.QuizCategoryActivity" />
        <activity android:name=".ui.activities.CollegeInfoActivity" />
        <activity android:name=".ui.activities.CollegeListActivity" />
        <activity android:name=".ui.activities.AboutQuizActivity" />
        <activity android:name=".ui.activities.SearchCategoryActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.VIBRATE" />

</manifest>